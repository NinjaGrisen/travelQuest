extends layout

block content
    .content__search
        input#searchForm(type="text" placeholder="Search for a city")
        each city in cities
            div.city-search
                a(href=`/city/${city}`)
                    span= city